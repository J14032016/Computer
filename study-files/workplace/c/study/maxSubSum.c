/*
  给定n个整数组成的序列，求该序列的子段和的最大值
  例： -6 2 6 -2
  得到 8 从第二个到第三个
 */
#include <stdio.h>

void maxSubSum(int * a, int array_size)
{
    
}

int main()
{
    int a[10];
    int array_size, i;
    printf("Number of the array : ");
    scanf("%d", &array_size);
    printf("input the element of the array : ");
    for(i=0; i<n; ++i)
        scanf("%d", &a[i]);
    maxSubSum(a, array_size);
    
    return 0;
}
